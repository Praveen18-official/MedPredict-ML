services:
  - type: web
    name: medpredict-ml-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m flask run --host=0.0.0.0 --port=$PORT
    healthCheckPath: /
    autoDeploy: true
